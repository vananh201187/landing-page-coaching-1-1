# Coaching 1-1 Landing Page

Static landing page for GitHub Pages.

## Deploy

Push this repository to GitHub, then in the repository settings set Pages source to **GitHub Actions**. The included workflow deploys `index.html` and `assets/van-anh.jpg` on every push to `main`.
